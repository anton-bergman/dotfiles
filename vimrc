" Enable syntax highlighting
syntax enable

" Enable line numbers
set number

" Enable relative line numbers
set relativenumber

" Define tab as 4 spaces
set tabstop=4
set softtabstop=4
set shiftwidth=4

" Enables smart indentation based on code structure
set smartindent

" Disable line wrapping, long lines will extend horizontally
set nowrap

" Enable incremental search, updating search results while typing
set incsearch

" Enable true color support (24-bit colors)
set termguicolors

" Set number of visible lines above/below the cursor while scrolling
set scrolloff=8